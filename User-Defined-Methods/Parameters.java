public class Parameters {
    /**
     * Parameters:
     * 
     * Need to add them to our method declaration in order to throw(pass) information
     * into a method. 
     * 
     * These are placed inside the declaration and must state the data type
     * as well as the parameter name.
     * 
     * The method below takes in two 'String' parameters: 'greeting' and
     * 'name'.
     * 
     * Parameters are treated like variables within the body of a method.
     */
    public static void exampleMethod(String greeting, String name){
        System.out.println(greeting + " " + name);
    }
    public static void main(String[] args){

    }
    
}
